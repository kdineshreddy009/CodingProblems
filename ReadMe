To pull the source folder to eclipse, the proper way of doing it it to right click in the package explorer and select "Import -> General -> Existing project" into the workspace and then import your projects. The presence of a project in the folder of the workspace is not enough to ensure that Eclipse will show you your projects. They need to be registered in the metadata of your workspace
